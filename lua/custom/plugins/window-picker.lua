return {
  's1n7ax/nvim-window-picker',
  opts = {
    hint = 'floating-big-letter',
    filter_rules = {
      include_current_win = true,
      autoselect_one = true,
    },
    selection_chars = 'JKL;ASDF',
  },
}
